python -m eel run.py gui --onefile --noconsole
pyinstaller run.spec


# -*- mode: python -*-

from distutils.sysconfig import get_python_lib
from os import path
skimage_plugins = Tree(
    path.join(get_python_lib(), "skimage","io","_plugins"),
    prefix=path.join("skimage","io","_plugins"),
    )

block_cipher = None


a = Analysis(['run.py'],
             pathex=['C:\\Users\\solar\\cytomata'],
             binaries=[],
             datas=[('C:\\Users\\solar\\anaconda3\\envs\\cy\\lib\\site-packages\\eel\\eel.js', 'eel'), ('gui', 'gui')],
             hiddenimports=['bottle_websocket', 'pywt._extensions._cwt', '_cffi_backend'],
             hookspath=[],
             runtime_hooks=[],
             excludes=[],
             win_no_prefer_redirects=False,
             win_private_assemblies=False,
             cipher=block_cipher,
             noarchive=False)
pyz = PYZ(a.pure, a.zipped_data,
             cipher=block_cipher)
exe = EXE(pyz,
          a.scripts,
          a.binaries,
          a.zipfiles,
          a.datas,
          skimage_plugins,
          [],
          name='run',
          debug=False,
          bootloader_ignore_signals=False,
          strip=False,
          upx=True,
          runtime_tmpdir=None,
          console=False,
          icon='media\\icon.ico')
